/**
 * Initialize the Concordium idle menu and display it to the 
 * user of the application. This should always be called at the
 * end of a successful command flow to reset the display correctly.
 */
void ui_idle(void);
