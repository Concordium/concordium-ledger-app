/**
 * Handles the signing flow, including updating the display, for the 'remove baker'
 * account transaction.
 * @param cdata please see /doc/ins_remove_baker for details
 */
void handleSignRemoveBaker(uint8_t *cdata, volatile unsigned int *flags);
